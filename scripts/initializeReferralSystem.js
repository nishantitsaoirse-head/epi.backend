const cron = require('node-cron');
const referralController = require('../controllers/referralController');

// Initialize the referral system
function initializeReferralSystem() {
}

module.exports = initializeReferralSystem; 